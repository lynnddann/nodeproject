<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/bootstrap/css/bootstrap.css' />
     <script type="text/javascript" src="/bootstrap/js/jquery-1.11.0.js"></script>
    <script type="text/javascript" src="/bootstrap/js/bootstrap.js"></script>
  </head>
  <body>
    <%- include('./header.ejs',{title:"店铺信息",isShow:false}) %>

    
    <div class="container">  
    <%if(isNew){%>  
    	<form class="form-horizontal" method="post" action="/goods" >
          <div class="form-group">
          <label style="font-size:16px">店铺信息：</label>
        </div>
       
        <div class="form-group">
          <label for="inputEmail3" class="col-sm-2 control-label">店铺名称：</label>
          <div class="col-sm-10">
            <input type="text" class="form-control" id="inputEmail3" placeholder="店铺名称" name="shopname">
          </div>
        </div>
        
        <div class="form-group">
          <label for="inputPassword3" class="col-sm-2 control-label">店铺介绍：</label>
          <div class="col-sm-10">
            <textarea class="form-control" rows="5" placeholder="字数不能超过100字" name="content"></textarea>
          </div>
        </div>
        <%if(isRepeat){%>
            <div class="form-group">
              <div class="col-sm-offset-2 col-sm-10">
                <label>店铺名称已存在，请重新输入</label>
              </div>         
            </div>
        <%}%>

        <div class="form-group">
          <div class="col-sm-offset-2 col-sm-10">
            <button type="submit" class="btn btn-default">提交</button>
          </div>
        </div>
      </form>

        <%} else {%>

      <form class="form-horizontal" method="post" action="/goods/fixed" >
          <div class="form-group">
          <label style="font-size:16px">店铺信息：</label>
        </div>
         
        <div class="form-group">
          <label for="inputEmail3" class="col-sm-2 control-label">店铺名称：</label>
          <div class="col-sm-10">
            <input type="text" class="form-control" id="inputEmail3" placeholder="店铺名称" name="shopname" value="<%= info.shopname%>">
          </div>
        </div>
        
        <div class="form-group">
          <label for="inputPassword3" class="col-sm-2 control-label">店铺介绍：</label>
          <div class="col-sm-10">
            <textarea class="form-control" rows="5" placeholder="字数不能超过100字" name="content"><%= info.content%></textarea>
          </div>
        </div>
        <%if(isRepeat){%>
            <div class="form-group">
              <div class="col-sm-offset-2 col-sm-10">
                <label>店铺名称已存在，请重新输入</label>
              </div>         
            </div>
        <%}%>

        <div class="form-group">
          <div class="col-sm-offset-2 col-sm-10">
          <input type="hidden" value="<%= info._id%>" name="id"/>
            <button type="submit" class="btn btn-default">修改</button>
          </div>
        </div>
      </form>
      <%}%>
    </div>
  </body>
</html>
